use crate::ssa::{SSA, SSAId};

pub struct Saturator {}

impl Saturator {
    pub fn make(&mut self, ssa: SSA) -> SSAId {
        todo!()
    }

    pub fn union(&mut self, a: SSAId, b: SSAId) -> SSAId {
        todo!()
    }
}
